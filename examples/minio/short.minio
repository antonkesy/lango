-- Point
data Point = MkPoint Float;
printPoint (MkPoint x) = show x;

-- Bool
isTrue True = "True";
isTrue False = "False";

-- List
l = [1, 2, 3];

main = do {
  putStr (isTrue True);

  let { point = MkPoint 3.0};
  putStr (printPoint point);

  putStr (show (length l));
};
