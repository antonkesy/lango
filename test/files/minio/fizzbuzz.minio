-- "1FizzBuzzFizzBuzz"
fizzbuzz n = if mod n 15 == 0 then "FizzBuzz"
              else if mod n 3 == 0 then "Fizz"
              else if mod n 5 == 0 then "Buzz"
              else show n;

main = do {
  putStr (fizzbuzz 1);
  putStr (fizzbuzz 3);
  putStr (fizzbuzz 5);
  putStr (fizzbuzz 15);
};
