-- RUN: OK
-- TYPECHECK: OK
-- "[1,3]"
myFilter p [] = [];
myFilter p (x:xs) = if p x then [x] ++ myFilter p xs else myFilter p xs;

isOdd x = (mod x 2) == 1;

main = do {
  putStr (show (myFilter isOdd [1,2,3,4]));
};
