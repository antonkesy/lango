-- RUN: OK
-- TYPECHECK: OK
-- "0,1,-1"
sign 0 = 0;
sign x = if x > 0 then 1 else (-1);

main = do {
  putStr (show (sign 0));
  putStr ",";
  putStr (show (sign 5));
  putStr ",";
  putStr (show (sign (-3)));
};
